<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internet permission for debug builds (Flutter dev tools + API calls) -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
